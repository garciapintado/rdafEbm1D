#module Sizes
#    implicit none
   
jmt <- 10 #   integer, parameter :: jmt = 20 ! number of grid points in the 
          #                         ! latitudinal direction (calculated 
          #                         ! points are from 2 through jmt-1. End
          #                         ! points are boundaries)

#end module Sizes
